---
phase: 04.1-model-visual-quality
plan: 05
type: execute
wave: 3
depends_on: ["04.1-01", "04.1-02", "04.1-03"]
files_modified:
  - visual/fp_arms.glb
  - visual/blender/make_fp_arms.py
  - src/first-person-weapon.js
autonomous: false

must_haves:
  truths:
    - "The first-person view shows realistic gloved/sleeved arms whose style matches the third-person soldier"
    - "The realistic rifle, pistol, and knife render correctly oriented in the first-person hand (forward, not pointing up/back)"
    - "FP arms carry team color"
  artifacts:
    - path: "visual/fp_arms.glb"
      provides: "Realistic first-person arms (derived from the soldier mesh) with a Hand.R bone for weapon attachment"
    - path: "visual/blender/make_fp_arms.py"
      provides: "Reproducible Blender script extracting FP arms from the soldier mesh"
    - path: "src/first-person-weapon.js"
      provides: "Per-weapon rotation/position offsets re-tuned for the realistic weapon models"
  key_links:
    - from: "src/first-person-weapon.js weaponOffsets"
      to: "visual/rifle.glb orientation"
      via: "rotation offset applied after GLTF import (replaces blanket -PI/2 Y assumption)"
      pattern: "weaponOffsets"
    - from: "visual/fp_arms.glb"
      to: "src/first-person-weapon.js handRBone lookup"
      via: "Hand.R bone"
      pattern: "Hand\\.R"
---

<objective>
Deliver realistic first-person arms and integrate the realistic weapon models into the existing two-pass FP weapon view. Produce `fp_arms.glb` (style-matched to the soldier, with a Hand.R bone), and re-tune the per-weapon rotation/position offsets in `src/first-person-weapon.js` for the new realistic rifle/pistol/knife (their axis conventions differ from the old X-axis Blender weapons — RESEARCH Pitfall 4).

Purpose: Satisfies VISUAL-02 (realistic FP arms matching the soldier) and the FP half of VISUAL-01/04 — weapons read realistically when held. Closes the cohesion requirement: opponent, arms, and weapons share one realistic style.

Output: `visual/fp_arms.glb` + `visual/blender/make_fp_arms.py`, and `src/first-person-weapon.js` with re-tuned weapon offsets.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04.1-model-visual-quality/04.1-RESEARCH.md
@.planning/phases/04.1-model-visual-quality/04.1-01-SUMMARY.md
@.planning/phases/04.1-model-visual-quality/04.1-02-SUMMARY.md
@.planning/phases/04.1-model-visual-quality/04.1-03-SUMMARY.md
@src/first-person-weapon.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Produce realistic FP arms (fp_arms.glb) matching the soldier</name>
  <files>visual/fp_arms.glb, visual/blender/make_fp_arms.py</files>
  <action>
Per RESEARCH "First-Person Arms" (recommended: derive from soldier mesh for style cohesion). Using the Blender MCP tools, write/run `visual/blender/make_fp_arms.py`:
1. Import the soldier source (the same FBX/Blender file from plan 01, or visual/soldier.glb).
2. Duplicate the mesh and isolate the arms (from approximately elbow/shoulder down through the hands/gloves) — delete the rest of the body. Add a simple cap or hide the cut seam if a gap shows at the shoulder (RESEARCH Open Q4).
3. Provide a minimal armature with bones named to match what `first-person-weapon.js` expects: it looks up a bone named exactly `Hand.R` for weapon attachment (FirstPersonWeapon.load traverses for `obj.isBone && obj.name === 'Hand.R'`). Include UpperArm/ForeArm/Hand bones for both arms; the Hand.R bone is the load-bearing one. Weight the arm mesh to these bones.
4. Export GLB to `visual/fp_arms.glb` (export_skins=True, export_yup=True). Keep the same filename — `FirstPersonWeapon.load('fp_arms.glb', ...)` is unchanged in main.js.
5. Back up the existing placeholder to `visual/fp_arms_placeholder.glb` first.

FALLBACK (RESEARCH): if cutting the soldier mesh produces unacceptable seams, a style-matched purchased FP-hands model is acceptable — record its license in visual/WEAPON_LICENSES.md if so.
  </action>
  <verify>
Inspect visual/fp_arms.glb with a GLTFLoader script: confirm it loads, contains a SkinnedMesh, and has a bone named exactly `Hand.R`. In-game, confirm the FP arms render in the bottom-right view with no console "Hand.R bone not found" error.
  </verify>
  <done>
visual/fp_arms.glb is realistic gloved/sleeved arms matching the soldier, contains a Hand.R bone, and loads without the "Hand.R not found" warning. make_fp_arms.py reproduces it.
  </done>
</task>

<task type="auto">
  <name>Task 2: Re-tune per-weapon offsets for the realistic weapon models</name>
  <files>src/first-person-weapon.js</files>
  <action>
The realistic rifle/pistol/knife GLBs (from plan 02) likely use a different axis convention than the old hand-built X-axis Blender weapons, so the blanket `rotation.y = -PI/2` assumption in `weaponOffsets` (lines ~42-46) will mis-orient them (RESEARCH Pitfall 4 — "weapon points at ceiling / muzzle behind camera").

1. For each weapon (rifle, pistol, knife), determine the correct orientation empirically. Temporarily attach a `THREE.AxesHelper` to the weapon model after load (RESEARCH "Weapon Rotation Calibration Pattern") to see its native forward axis when parented to Hand.R.
2. Update the `weaponOffsets` entries (pos + rot) per weapon so each points forward (down -Z from the camera), sits naturally in the hand, and the barrel/blade orientation is correct. These replace the placeholder-era offsets.
3. Update `_updateMuzzleFlashPosition()` muzzle positions (rifle/pistol) so the muzzle flash sits at the actual barrel end of the new realistic models (the old positions like rifle [0,-0.04,-0.55] were tuned to the placeholder barrel length).
4. Remove the AxesHelper before finishing.
5. Confirm the knife uses no muzzle flash (existing fire() already early-returns for knife) and the realistic knife is oriented blade-forward.

Do not change the bob/recoil/reload animation logic — only the static per-weapon orientation/position offsets and muzzle positions.
  </action>
  <verify>
In-game, switch through rifle (1), pistol (2), knife (3): each weapon is held forward in a believable grip, not pointing up/back/sideways. Fire the rifle and pistol — muzzle flash appears at the barrel end. AxesHelper removed. No console errors.
  </verify>
  <done>
All three realistic weapons render correctly oriented in the FP hand with correct muzzle-flash positions; offsets re-tuned in weaponOffsets; calibration helper removed.
  </done>
</task>

</tasks>

<verification>
- fp_arms.glb is realistic, style-matched, has a Hand.R bone, loads without warning
- Rifle/pistol/knife each oriented correctly in the FP hand
- Muzzle flash at the correct barrel end per weapon
- FP arms carry team color (existing setTeamColor path still works)
- No console errors; 60fps maintained
</verification>

<success_criteria>
1. FP arms read as realistic gloved/sleeved arms matching the soldier (VISUAL-02)
2. Realistic rifle/pistol/knife render correctly oriented when held (VISUAL-01 FP)
3. Opponent + arms + weapons cohere in one realistic style (VISUAL-04)
4. Existing FP systems (bob, recoil, reload, team color, two-pass render) still work
</success_criteria>

<output>
After completion, create `.planning/phases/04.1-model-visual-quality/04.1-05-SUMMARY.md`
</output>
