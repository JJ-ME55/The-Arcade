export function Wager() {
  return (
    <main style={{ padding: 'var(--space-8)', maxWidth: 720, margin: '0 auto' }}>
      <h1 style={{ color: 'var(--accent)', marginBottom: 'var(--space-6)' }}>
        Wager Mode — Coming Soon
      </h1>
      <p style={{ opacity: 0.85, marginBottom: 'var(--space-6)' }}>
        Multi-game time-windowed wagers. Pick the games, set the window, top score wins the pot.
        Smart-contract escrow, automatic on-chain payout.
      </p>
      <p style={{ opacity: 0.7 }}>Waitlist form lands here once the email-capture endpoint is wired.</p>
    </main>
  );
}
